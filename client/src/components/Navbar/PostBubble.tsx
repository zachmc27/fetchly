// the floating post bubble who's functionality changes based on what page the user is on:
// render NewPost component if on home page
// render NewAdoptionPost component if on adoption page
// render NewMeetupPost component if on meetup page
// render NewMessage if on inbox page

export default function PostBubble() {
  return (
    <div>PostBubble</div>
  )
}
